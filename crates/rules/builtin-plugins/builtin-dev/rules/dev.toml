id = "builtin-dev"
name = "Built-in developer caches"
version = "0.17.0"
description = "Generated dependencies, build artifacts, and review-required data for developer projects."
categories = ["developer-cache", "build-cache", "package-cache"]

[[sources]]
id = "kondo"
repository = "https://github.com/tbillington/kondo"
revision = "1d351ca80b3d3adfad9bbe7db872c27359190210"
license = "MIT"
relation = "adapted"

[[sources]]
id = "dusty"
repository = "https://github.com/yagcioglutoprak/dusty"
revision = "acaa3f6a5e9b535892ce2301b0a57772a0ee430a"
license = "MIT"
relation = "adapted"

[[sources]]
id = "puremac"
repository = "https://github.com/momenbasel/PureMac"
revision = "376b2380b0bffe7d614258731a58732ed65d563b"
license = "MIT"
relation = "adapted"

# Cross-stack project artifact paths were audited against the MIT-licensed
# https://github.com/tbillington/kondo source at commit
# 1d351ca80b3d3adfad9bbe7db872c27359190210. Confidence, selection, and risk
# semantics below are Cleanr-specific; intentionally created `.venv` directories
# are excluded.
#
# User-level macOS developer cache paths were also audited against the
# MIT-licensed https://github.com/yagcioglutoprak/dusty source at commit
# acaa3f6a5e9b535892ce2301b0a57772a0ee430a and corroborated with the
# MIT-licensed https://github.com/momenbasel/PureMac source at commit
# 376b2380b0bffe7d614258731a58732ed65d563b.

[[rules]]
id = "node-modules"
label = "Node dependency directory"
category = "developer-cache"
match = { dir_name = "node_modules", min_size = 1048576 }
confidence = "high"
default_selected = true
source_refs = ["kondo"]
action = "trash"
reason = "node_modules is a generated dependency directory that package managers can recreate."
risk_note = "Project dependencies will need to be reinstalled before the project can run again."

[[rules]]
id = "node-angular-cache"
label = "Angular project cache"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], excluded_root_dir_globs = ["ios", "android"], artifact_paths = [".angular"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .angular directory contains generated Angular tooling and build cache data."
risk_note = "The next Angular build or development-server start may take longer."

[[rules]]
id = "react-native-android-build-cache"
label = "React Native Android build data"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], root_dir_globs = ["ios", "android"], artifact_paths = ["android/build", "android/.gradle"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain generated Gradle build outputs and project-local cache data."
risk_note = "The next Gradle build may require network access and take longer."

[[rules]]
id = "nuxt-generated-cache"
label = "Nuxt generated cache"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], excluded_root_dir_globs = ["ios", "android"], artifact_paths = [".nuxt"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .nuxt directory contains framework-generated build data and caches."
risk_note = "The next Nuxt development start or build may take longer while the directory is regenerated."

[[rules]]
id = "nuxt-output"
label = "Nuxt output directory"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], excluded_root_dir_globs = ["ios", "android"], artifact_paths = [".output"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The .output directory is normally produced by a Nuxt production build."
risk_note = "Review before cleaning because it may be the retained artifact currently used for deployment."

[[rules]]
id = "sveltekit-generated-cache"
label = "SvelteKit generated cache"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], excluded_root_dir_globs = ["ios", "android"], artifact_paths = [".svelte-kit"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .svelte-kit directory contains generated framework output and tooling metadata."
risk_note = "The next SvelteKit development start or build may take longer while it is regenerated."

[[rules]]
id = "astro-generated-cache"
label = "Astro generated cache"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], excluded_root_dir_globs = ["ios", "android"], artifact_paths = [".astro"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .astro directory contains generated type and framework cache data."
risk_note = "Astro will regenerate the directory; the next tooling or build run may take longer."

[[rules]]
id = "parcel-project-cache"
label = "Parcel project cache"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], excluded_root_dir_globs = ["ios", "android"], artifact_paths = [".parcel-cache"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .parcel-cache directory contains generated bundler cache data."
risk_note = "The next Parcel build may take longer while assets are rebuilt."

[[rules]]
id = "javascript-test-coverage"
label = "JavaScript test coverage output"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], artifact_paths = ["coverage"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The coverage directory is normally generated by a JavaScript test run."
risk_note = "Review before cleaning because teams may retain coverage output for comparison, reports, or CI artifacts."

[[rules]]
id = "react-native-ios-build-cache"
label = "React Native iOS build data"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], root_dir_globs = ["ios", "android"], artifact_paths = ["ios/build", "ios/DerivedData"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain generated iOS build products and derived data."
risk_note = "The next iOS build and indexing pass may take longer."

[[rules]]
id = "react-native-metro-cache"
label = "React Native Metro cache"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], root_dir_globs = ["ios", "android"], artifact_paths = [".metro"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .metro directory contains generated React Native bundler cache data."
risk_note = "Metro will reprocess project modules before cached results are available again."

[[rules]]
id = "react-native-expo-state"
label = "Expo local project state"
category = "developer-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], root_dir_globs = ["ios", "android"], artifact_paths = [".expo"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The .expo directory contains generated, machine-local project state."
risk_note = "Review before deleting because local device and development-server state will be reset."

[[rules]]
id = "react-native-pods"
label = "React Native CocoaPods dependencies"
category = "package-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["package.json"], root_dir_globs = ["ios", "android"], artifact_paths = ["ios/Pods"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The Pods directory contains installed project dependencies."
risk_note = "Dependencies must be installed again and may require network access, credentials, or package sources that are no longer available."

[[rules]]
id = "rust-target"
label = "Rust target directory"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["Cargo.toml"], artifact_paths = ["target"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "Rust target directories contain generated build artifacts."
risk_note = "The next cargo build may take longer."

[[rules]]
id = "cargo-xwin-cache"
label = "Cargo xwin toolchain cache"
category = "package-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["Cargo.toml"], artifact_paths = [".xwin-cache"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The .xwin-cache directory contains downloaded cross-compilation toolchain data."
risk_note = "Rebuilding may require a large download, network access, and additional setup time."

[[rules]]
id = "python-pycache"
label = "Python bytecode cache"
category = "developer-cache"
match = { dir_name = "__pycache__" }
confidence = "high"
default_selected = true
action = "trash"
reason = "Python bytecode caches are regenerated automatically."
risk_note = "Python may recompile modules on the next run."

[[rules]]
id = "python-pytest-cache"
label = "pytest cache"
category = "developer-cache"
match = { dir_name = ".pytest_cache" }
confidence = "high"
default_selected = true
action = "trash"
reason = "pytest caches test metadata and can recreate it."
risk_note = "The next test run may lose cached state."

[[rules]]
id = "python-mypy-cache"
label = "mypy cache"
category = "developer-cache"
match = { dir_name = ".mypy_cache" }
confidence = "high"
default_selected = true
action = "trash"
reason = "mypy cache directories are generated type-checking artifacts."
risk_note = "The next mypy run may be slower."

[[rules]]
id = "python-ruff-cache"
label = "ruff cache"
category = "developer-cache"
match = { dir_name = ".ruff_cache" }
confidence = "high"
default_selected = true
action = "trash"
reason = "ruff cache directories are generated linting artifacts."
risk_note = "The next ruff run may be slower."

[[rules]]
id = "tox-cache"
label = "tox environments"
category = "developer-cache"
match = { dir_name = ".tox", min_size = 1048576 }
confidence = "medium"
default_selected = false
action = "trash"
reason = "tox environments are generated, but deleting them may remove expensive local environments."
risk_note = "Review before deleting; environments may take time or network access to rebuild."

[[rules]]
id = "python-nox-environments"
label = "nox environments"
category = "developer-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["*.py"], artifact_paths = [".nox"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "nox environments are generated from project session configuration."
risk_note = "Review before deleting because recreating environments may require network access and significant setup time."

[[rules]]
id = "python-local-packages"
label = "Python local package environment"
category = "package-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["*.py"], artifact_paths = ["__pypackages__"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The __pypackages__ directory contains locally installed project dependencies."
risk_note = "Dependencies must be installed again and may require network access or unavailable package sources."

[[rules]]
id = "unity-generated-cache"
label = "Unity generated project caches"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["Assembly-CSharp.csproj"], artifact_paths = ["Library", "Temp", "Obj"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain generated imports, intermediate files, and build cache data."
risk_note = "The editor will regenerate the data, which can make the next project open or build substantially slower."

[[rules]]
id = "unity-logs"
label = "Unity project logs"
category = "developer-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["Assembly-CSharp.csproj"], artifact_paths = ["Logs"] } }
confidence = "low"
default_selected = false
action = "trash"
reason = "The Logs directory contains diagnostic output produced while developing the project."
risk_note = "Deleting it removes diagnostic history that may still be needed to investigate failures."

[[rules]]
id = "unity-memory-captures"
label = "Unity memory captures"
category = "developer-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["Assembly-CSharp.csproj"], artifact_paths = ["MemoryCaptures"] } }
confidence = "low"
default_selected = false
action = "trash"
reason = "Memory captures are large, intentionally recorded diagnostic artifacts."
risk_note = "Captures may be irreplaceable and can contain sensitive application data; inspect them before deleting."

[[rules]]
id = "unity-build-output"
label = "Unity build outputs"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["Assembly-CSharp.csproj"], artifact_paths = ["Build", "Builds"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "Build and Builds directories commonly contain generated player outputs."
risk_note = "Review before deleting because these may be retained release artifacts that are expensive or impossible to reproduce exactly."

[[rules]]
id = "haskell-stack-work"
label = "Haskell Stack build directory"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["stack.yaml"], artifact_paths = [".stack-work"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .stack-work directory contains generated Stack build artifacts."
risk_note = "The next Stack build may take longer and may need to fetch dependencies."

[[rules]]
id = "haskell-cabal-dist"
label = "Cabal build directory"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["cabal.project"], artifact_paths = ["dist-newstyle"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The dist-newstyle directory contains generated Cabal build artifacts."
risk_note = "The next Cabal build may take longer and may need to fetch dependencies."

[[rules]]
id = "sbt-target"
label = "sbt target directories"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["build.sbt"], artifact_paths = ["target", "project/target"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These target directories contain generated Scala build artifacts and metadata."
risk_note = "The next sbt build may take longer."

[[rules]]
id = "maven-target"
label = "Maven target directory"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["pom.xml"], artifact_paths = ["target"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The target directory contains generated Maven build artifacts."
risk_note = "The next Maven build may take longer."

[[rules]]
id = "gradle-project-artifacts"
label = "Gradle project build data"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["build.gradle", "build.gradle.kts"], artifact_paths = ["build", ".gradle"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain generated Gradle build outputs and project-local cache data."
risk_note = "The next Gradle build may require network access and take longer."

[[rules]]
id = "cmake-build-output"
label = "CMake build directories"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["CMakeLists.txt"], artifact_paths = ["build", "cmake-build-debug", "cmake-build-release"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain generated CMake configuration and compiler outputs."
risk_note = "The project must be configured and compiled again."

[[rules]]
id = "android-native-build-cache"
label = "Android native build cache"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["build.gradle", "build.gradle.kts"], artifact_paths = [".cxx", ".externalNativeBuild"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain generated CMake and native Android build intermediates."
risk_note = "The next native Android build will regenerate them and may take substantially longer."

[[rules]]
id = "unreal-generated-cache"
label = "Unreal generated project caches"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["*.uproject"], artifact_paths = ["DerivedDataCache", "Intermediate"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain derived asset data and generated build intermediates."
risk_note = "Shaders, assets, and project files may need expensive regeneration on the next open or build."

[[rules]]
id = "unreal-binaries"
label = "Unreal project binaries"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["*.uproject"], artifact_paths = ["Binaries"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The Binaries directory commonly contains compiled project outputs."
risk_note = "Review before deleting because rebuilding can be expensive and the directory may contain retained deliverables."

[[rules]]
id = "unreal-build-data"
label = "Unreal project build data"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["*.uproject"], artifact_paths = ["Build"] } }
confidence = "low"
default_selected = false
action = "trash"
reason = "The Build directory can contain platform-specific build resources and generated packaging data."
risk_note = "Do not delete without inspection because some files may be required inputs rather than reproducible cache data."

[[rules]]
id = "unreal-saved-data"
label = "Unreal project saved data"
category = "developer-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["*.uproject"], artifact_paths = ["Saved"] } }
confidence = "low"
default_selected = false
action = "trash"
reason = "The Saved directory contains engine-managed autosaves, configuration, logs, and crash data."
risk_note = "Deleting it can remove recovery data and project-local settings; inspect its contents first."

[[rules]]
id = "jupyter-checkpoints"
label = "Jupyter notebook checkpoints"
category = "developer-cache"
match = { kind = "directory", project = { marker_globs = ["*.ipynb"], artifact_paths = [".ipynb_checkpoints"] } }
confidence = "low"
default_selected = false
action = "trash"
reason = "Notebook checkpoint directories hold automatically created recovery copies."
risk_note = "Deleting checkpoints removes rollback and recovery options for notebook content."

[[rules]]
id = "pixi-environment"
label = "Pixi project environment"
category = "package-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["pixi.toml"], artifact_paths = [".pixi"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The .pixi directory contains an installed, project-local environment."
risk_note = "Recreating the environment may require large downloads, network access, credentials, and significant time."

[[rules]]
id = "composer-vendor"
label = "Composer dependency directory"
category = "package-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["composer.json"], artifact_paths = ["vendor"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The vendor directory contains installed project dependencies."
risk_note = "Dependencies must be installed again and may include local modifications or packages that are no longer available."

[[rules]]
id = "dart-tooling-cache"
label = "Dart and Flutter tooling data"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["pubspec.yaml"], artifact_paths = [".dart_tool", "linux/flutter/ephemeral", "windows/flutter/ephemeral"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain generated package configuration and platform tooling data."
risk_note = "Tooling will regenerate the data and the next dependency resolution or build may take longer."

[[rules]]
id = "dart-build-output"
label = "Dart and Flutter build output"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["pubspec.yaml"], artifact_paths = ["build"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The build directory commonly contains generated application and package outputs."
risk_note = "Review before deleting because retained distributable artifacts may be expensive to reproduce exactly."

[[rules]]
id = "elixir-build-cache"
label = "Elixir build directory"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["mix.exs"], artifact_paths = ["_build"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The _build directory contains generated Mix build artifacts."
risk_note = "The next Mix build may take longer."

[[rules]]
id = "elixir-language-server-cache"
label = "Elixir language-server caches"
category = "developer-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["mix.exs"], artifact_paths = [".elixir_ls", ".lexical"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain generated language-server indexes and build data."
risk_note = "Editor intelligence may be unavailable or slower until the indexes are rebuilt."

[[rules]]
id = "elixir-tools"
label = "Elixir project tools"
category = "developer-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["mix.exs"], artifact_paths = [".elixir-tools"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The .elixir-tools directory contains project-local editor tooling artifacts."
risk_note = "Recreating installed tools may require downloads, credentials, and additional setup time."

[[rules]]
id = "swift-build-cache"
label = "Swift package build directory"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["Package.swift"], artifact_paths = [".build"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .build directory contains generated Swift package build products and checkouts."
risk_note = "The next package build may require dependency downloads and take longer."

[[rules]]
id = "swiftpm-state"
label = "Swift package-manager state"
category = "developer-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["Package.swift"], artifact_paths = [".swiftpm"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The .swiftpm directory contains package-manager and IDE workspace state."
risk_note = "Review before deleting because local workspace configuration may be reset."

[[rules]]
id = "zig-cache"
label = "Zig build caches"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["build.zig"], artifact_paths = ["zig-cache", ".zig-cache"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "These directories contain generated Zig compilation cache data."
risk_note = "The next Zig build may take longer."

[[rules]]
id = "zig-output"
label = "Zig install output"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["build.zig"], artifact_paths = ["zig-out"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The zig-out directory contains generated install and build outputs."
risk_note = "Review before deleting because retained binaries or distributable artifacts must be rebuilt."

[[rules]]
id = "godot-import-cache"
label = "Godot imported asset cache"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["project.godot"], artifact_paths = [".godot"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .godot directory contains generated imports and editor cache data."
risk_note = "The editor will reimport assets and rebuild cache data, which may take substantial time."

[[rules]]
id = "dotnet-intermediate-output"
label = ".NET intermediate output"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["*.csproj", "*.fsproj"], excluded_marker_globs = ["project.godot", "Assembly-CSharp.csproj"], artifact_paths = ["obj"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The obj directory contains generated compiler intermediates and restore metadata."
risk_note = "The next restore or build may take longer."

[[rules]]
id = "dotnet-binary-output"
label = ".NET binary output"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["*.csproj", "*.fsproj"], excluded_marker_globs = ["project.godot", "Assembly-CSharp.csproj"], artifact_paths = ["bin"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The bin directory commonly contains generated compiled outputs."
risk_note = "Review before deleting because retained binaries and distributable artifacts must be rebuilt."

[[rules]]
id = "turborepo-cache"
label = "Turborepo cache"
category = "build-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["turbo.json"], artifact_paths = [".turbo"] } }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .turbo directory contains generated task outputs, logs, and cache metadata."
risk_note = "Tasks will run again instead of using cached results."

[[rules]]
id = "terraform-working-data"
label = "Terraform working-directory data"
category = "developer-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = [".terraform.lock.hcl"], artifact_paths = [".terraform"] } }
confidence = "low"
default_selected = false
action = "trash"
reason = "The .terraform directory mixes downloaded providers and modules with workspace and backend metadata."
risk_note = "Deleting it resets initialization state and may remove sensitive backend settings; inspect it and preserve state files before proceeding."

[[rules]]
id = "cocoapods-dependencies"
label = "CocoaPods dependency directory"
category = "package-cache"
match = { kind = "directory", min_size = 1048576, project = { marker_globs = ["Podfile"], artifact_paths = ["Pods"] } }
confidence = "medium"
default_selected = false
action = "trash"
reason = "The Pods directory contains installed project dependencies."
risk_note = "Dependencies must be installed again and may require network access, credentials, or package sources that are no longer available."

[[rules]]
id = "next-cache"
label = "Next.js build cache"
category = "build-cache"
match = { path_glob = "**/.next/cache", min_size = 1048576 }
confidence = "high"
default_selected = true
action = "trash"
reason = "The .next/cache directory contains generated Next.js build cache data."
risk_note = "The next Next.js build may be slower."

[[rules]]
id = "cargo-registry"
label = "Cargo registry cache"
category = "package-cache"
match = { path_glob = "**/.cargo/registry", min_size = 1048576 }
confidence = "high"
default_selected = true
action = "trash"
reason = "Cargo registry sources and package archives can be downloaded again."
risk_note = "Future Cargo builds may need network access and take longer."

[[rules]]
id = "cargo-git"
label = "Cargo Git dependency cache"
category = "package-cache"
match = { path_glob = "**/.cargo/git", min_size = 1048576 }
confidence = "high"
default_selected = true
action = "trash"
reason = "Cargo Git dependency checkouts are generated package-manager caches."
risk_note = "Future Cargo builds may need to clone dependencies again."

[[rules]]
id = "npm-cache"
label = "npm cache"
category = "package-cache"
match = { path_glob = "**/.npm", min_size = 1048576 }
confidence = "high"
default_selected = true
action = "trash"
reason = "The npm cache contains downloaded package data that npm can recreate."
risk_note = "Future npm installs may need network access."

[[rules]]
id = "tool-download-cache"
label = "Developer package download cache"
category = "package-cache"
match = { path_glob = "**/.cache/{pnpm,yarn,pip}", min_size = 1048576 }
confidence = "high"
default_selected = true
action = "trash"
reason = "Package-manager download caches can be recreated from their registries."
risk_note = "Future dependency installs may need network access and take longer."

[[rules]]
id = "macos-tool-download-cache"
label = "macOS developer package cache"
category = "package-cache"
match = { path_glob = "**/Library/Caches/{Yarn,pip}", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "Package-manager download caches can be recreated from their registries."
risk_note = "Future dependency installs may need network access and take longer."

[[rules]]
id = "corepack-download-cache"
label = "Corepack download cache"
category = "package-cache"
match = { kind = "directory", path_glob = "**/{.cache,Library/Caches}/node/corepack", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos", "linux"]
action = "trash"
reason = "Corepack stores downloaded package-manager releases in this cache and can fetch them again."
risk_note = "A future package-manager launch may require network access and take longer."

[[rules]]
id = "windows-corepack-download-cache"
label = "Corepack download cache"
category = "package-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/node/corepack", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["windows"]
action = "trash"
reason = "Corepack stores downloaded package-manager releases in this cache and can fetch them again."
risk_note = "A future package-manager launch may require network access and take longer."

[[rules]]
id = "language-version-manager-download-cache"
label = "Language version manager download cache"
category = "package-cache"
match = { kind = "directory", path_glob = "**/{.pyenv,.rbenv}/cache", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos", "linux"]
action = "trash"
reason = "These directories contain downloaded language runtime archives retained by version managers."
risk_note = "Installing those runtime versions again may require network access and large downloads."

[[rules]]
id = "rustup-download-cache"
label = "rustup download cache"
category = "package-cache"
match = { kind = "directory", path_glob = "**/.rustup/downloads", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos", "windows", "linux"]
action = "trash"
reason = "The rustup downloads directory contains retrievable toolchain archives and partial download state."
risk_note = "Future toolchain installation may need network access; do not clean while rustup is running."
runtime_guard = { process_names = ["rustup"] }

[[rules]]
id = "pre-commit-environments"
label = "pre-commit managed environments"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/pre-commit", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux"]
action = "trash"
reason = "pre-commit creates managed hook environments under its cache directory."
risk_note = "Review before cleaning because hooks may require network access and significant setup time to recreate."

[[rules]]
id = "pnpm-store"
label = "pnpm content-addressable store"
category = "package-cache"
match = { path_glob = "**/{.local/share/pnpm,Library/pnpm}/store", min_size = 1048576 }
confidence = "high"
default_selected = true
action = "trash"
reason = "The pnpm store contains downloaded package content that pnpm can recreate."
risk_note = "Future pnpm installs may need network access."

[[rules]]
id = "windows-npm-cache"
label = "npm cache"
category = "package-cache"
match = { dir_name = "npm-cache", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["windows"]
action = "trash"
reason = "The npm cache contains downloaded package data that npm can recreate."
risk_note = "Future npm installs may need network access."

[[rules]]
id = "windows-pnpm-store"
label = "pnpm content-addressable store"
category = "package-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/pnpm/store", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["windows"]
action = "trash"
reason = "The pnpm store contains downloaded package content that pnpm can recreate."
risk_note = "Future pnpm installs may need network access and take longer."

[[rules]]
id = "nuget-package-cache"
label = "NuGet global package cache"
category = "package-cache"
match = { kind = "directory", path_glob = "**/.nuget/packages", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos", "windows", "linux"]
action = "trash"
reason = "The NuGet global packages folder normally contains downloaded and extracted package content."
risk_note = "Review before cleaning because restoring packages may require network access or package sources that are no longer available."

[[rules]]
id = "bun-package-cache"
label = "Bun package cache"
category = "package-cache"
match = { kind = "directory", path_glob = "**/.bun/install/cache", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos", "linux"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "Bun's install cache contains downloaded packages that Bun can fetch again."
risk_note = "Future Bun installs may require network access and take longer."

[[rules]]
id = "gradle-cache"
label = "Gradle cache"
category = "build-cache"
match = { path_glob = "**/.gradle/caches", min_size = 1048576 }
confidence = "high"
default_selected = true
action = "trash"
reason = "Gradle caches downloaded dependencies and generated build metadata."
risk_note = "The next Gradle build may need network access and take longer."

[[rules]]
id = "maven-repository"
label = "Maven local repository"
category = "package-cache"
match = { path_glob = "**/.m2/repository", min_size = 1048576 }
confidence = "medium"
default_selected = false
action = "trash"
reason = "Most Maven repository content can be downloaded again."
risk_note = "The repository may contain locally installed artifacts that are not available remotely."

[[rules]]
id = "go-module-cache"
label = "Go module cache"
category = "package-cache"
match = { path_glob = "**/go/pkg/mod", min_size = 1048576 }
confidence = "high"
default_selected = true
action = "trash"
reason = "Downloaded Go modules can be fetched again."
risk_note = "Future Go builds may need network access."

[[rules]]
id = "xcode-derived-data"
label = "Xcode DerivedData"
category = "build-cache"
match = { path_glob = "**/Library/Developer/Xcode/DerivedData", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "Xcode DerivedData contains generated indexes and build products."
risk_note = "Xcode will rebuild indexes and products, which may take time."
runtime_guard = { process_names = ["Xcode", "xcodebuild"] }

[[rules]]
id = "macos-package-manager-cache"
label = "macOS developer tool cache"
category = "package-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/{Homebrew,CocoaPods,org.swift.swiftpm,deno,composer}", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "These package-manager and developer-tool caches contain downloads or generated metadata that their owning tools can recreate."
risk_note = "Future installs or tool runs may require network access and take longer."

[[rules]]
id = "macos-home-directory-tool-cache"
label = "macOS home-directory developer cache"
category = "package-cache"
match = { kind = "directory", path_glob = "**/{.pub-cache,.yarn/cache}", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "These developer-tool directories contain downloaded packages that their owning tools can recreate."
risk_note = "Future installs or builds may require network access and take longer."

[[rules]]
id = "macos-go-build-cache"
label = "Go build cache"
category = "build-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/go-build", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "The Go build cache contains generated compiler artifacts."
risk_note = "The next Go build may take longer while artifacts are rebuilt."

[[rules]]
id = "xcode-coresimulator-cache"
label = "Xcode CoreSimulator cache"
category = "build-cache"
match = { kind = "directory", path_glob = "**/Library/Developer/CoreSimulator/Caches", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "CoreSimulator caches contain generated simulator support data."
risk_note = "Quit Xcode and Simulator first; simulator startup may be slower while cache data is rebuilt."
runtime_guard = { process_names = ["Xcode", "xcodebuild", "Simulator", "simctl"] }

[[rules]]
id = "xcode-application-cache"
label = "Xcode application cache"
category = "build-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/com.apple.dt.Xcode", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "Xcode's Library/Caches directory contains generated IDE cache data."
risk_note = "Quit Xcode first; its next launch and indexing pass may take longer."
runtime_guard = { process_names = ["Xcode", "xcodebuild"] }

[[rules]]
id = "xcode-preview-cache"
label = "Xcode preview cache"
category = "build-cache"
match = { kind = "directory", path_glob = "**/Library/Developer/Xcode/UserData/Previews", min_size = 1048576 }
confidence = "high"
default_selected = true
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "Xcode preview data contains generated SwiftUI preview build products."
risk_note = "Quit Xcode first; previews will be rebuilt and may take longer to render initially."
runtime_guard = { process_names = ["Xcode", "xcodebuild"] }

[[rules]]
id = "xcode-device-support"
label = "Xcode device support version"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Developer/Xcode/{iOS,watchOS,tvOS} DeviceSupport/*", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "Xcode downloads device support symbols for each connected operating-system version."
risk_note = "Review before cleaning; symbols may be needed to inspect or symbolicate existing device crash reports and can require a large download to restore."
runtime_guard = { process_names = ["Xcode", "xcodebuild"] }

[[rules]]
id = "xcode-test-devices"
label = "Xcode test devices"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Developer/XCTestDevices/*", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "XCTestDevices contains simulator clones and local state created by Xcode test runs."
risk_note = "Review before cleaning and quit Xcode; test devices and their state will need to be recreated."
runtime_guard = { process_names = ["Xcode", "xcodebuild", "xctest"] }

[[rules]]
id = "xcode-archives"
label = "Xcode archive"
category = "developer-cache"
match = { kind = "directory", extension = "xcarchive", path_glob = "**/Library/Developer/Xcode/Archives/**", min_size = 1048576 }
confidence = "low"
default_selected = false
platforms = ["macos"]
source_refs = ["dusty", "puremac"]
action = "trash"
reason = "An Xcode archive contains a retained application build and can consume substantial disk space."
risk_note = "Archives may contain irreplaceable builds and dSYMs needed to redistribute or symbolicate released applications; inspect individual archives before cleaning."
runtime_guard = { process_names = ["Xcode", "xcodebuild"] }

# Standard cache paths verified against upstream documentation in September 2026.
# See docs/docs/rules/cache-expansion.md for sources and retained-data boundaries.

[[rules]]
id = "macos-playwright-cache"
label = "Playwright download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/ms-playwright", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "trash"
reason = "Playwright stores downloaded runtime binaries in this standard user cache."
risk_note = "Review the retained versions and quit dependent tools first; binaries may be shared by projects and require a large download before offline work can resume."
runtime_guard = { process_names = ["node", "bun", "deno", "python", "java", "dotnet", "chrome", "chromium", "headless_shell", "firefox", "webkit", "MiniBrowser", "Playwright", "ffmpeg", "chrome-headless-shell", "WebKitWebProcess"] }
[[rules]]
id = "macos-electron-cache"
label = "Electron download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/electron", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "trash"
reason = "Electron stores downloaded runtime binaries in this standard user cache."
risk_note = "Review the retained versions and quit dependent tools first; binaries may be shared by projects and require a large download before offline work can resume."
runtime_guard = { process_names = ["node", "bun", "npm", "electron"] }
[[rules]]
id = "macos-cypress-cache"
label = "Cypress download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/Cypress", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "trash"
reason = "Cypress stores downloaded runtime binaries in this standard user cache."
risk_note = "Review the retained versions and quit dependent tools first; binaries may be shared by projects and require a large download before offline work can resume."
runtime_guard = { process_names = ["node", "Cypress"] }
[[rules]]
id = "linux-playwright-cache"
label = "Playwright download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/ms-playwright", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "trash"
reason = "Playwright stores downloaded runtime binaries in this standard user cache."
risk_note = "Review the retained versions and quit dependent tools first; binaries may be shared by projects and require a large download before offline work can resume."
runtime_guard = { process_names = ["node", "bun", "deno", "python", "java", "dotnet", "chrome", "chromium", "headless_shell", "firefox", "webkit", "MiniBrowser", "Playwright", "ffmpeg", "chrome-headless-shell", "WebKitWebProcess"] }
[[rules]]
id = "linux-electron-cache"
label = "Electron download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/electron", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "trash"
reason = "Electron stores downloaded runtime binaries in this standard user cache."
risk_note = "Review the retained versions and quit dependent tools first; binaries may be shared by projects and require a large download before offline work can resume."
runtime_guard = { process_names = ["node", "bun", "npm", "electron"] }
[[rules]]
id = "linux-cypress-cache"
label = "Cypress download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/Cypress", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "trash"
reason = "Cypress stores downloaded runtime binaries in this standard user cache."
risk_note = "Review the retained versions and quit dependent tools first; binaries may be shared by projects and require a large download before offline work can resume."
runtime_guard = { process_names = ["node", "Cypress"] }
[[rules]]
id = "windows-playwright-cache"
label = "Playwright download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/ms-playwright", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "trash"
reason = "Playwright stores downloaded runtime binaries in this standard user cache."
risk_note = "Review the retained versions and quit dependent tools first; binaries may be shared by projects and require a large download before offline work can resume."
runtime_guard = { process_names = ["node", "bun", "deno", "python", "java", "dotnet", "chrome", "chromium", "headless_shell", "firefox", "webkit", "MiniBrowser", "Playwright", "ffmpeg", "chrome-headless-shell", "WebKitWebProcess"] }
[[rules]]
id = "windows-electron-cache"
label = "Electron download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/electron/Cache", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "trash"
reason = "Electron stores downloaded runtime binaries in this standard user cache."
risk_note = "Review the retained versions and quit dependent tools first; binaries may be shared by projects and require a large download before offline work can resume."
runtime_guard = { process_names = ["node", "bun", "npm", "electron"] }
[[rules]]
id = "windows-cypress-cache"
label = "Cypress download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/Cypress/Cache", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "trash"
reason = "Cypress stores downloaded runtime binaries in this standard user cache."
risk_note = "Review the retained versions and quit dependent tools first; binaries may be shared by projects and require a large download before offline work can resume."
runtime_guard = { process_names = ["node", "Cypress"] }
[[rules]]
id = "puppeteer-browser-cache"
label = "Puppeteer browser downloads"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/puppeteer", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "trash"
reason = "Puppeteer downloads managed browser versions into its default user cache."
risk_note = "Quit browser automation and review retained versions; projects may need to download browsers again before they can run."
runtime_guard = { process_names = ["node", "bun", "deno", "python", "java", "dotnet", "chrome", "chromium", "headless_shell", "firefox", "webkit", "MiniBrowser", "Playwright", "ffmpeg", "chrome-headless-shell", "WebKitWebProcess"] }
[[rules]]
id = "linux-go-build-cache"
label = "Go build cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/go-build", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "trash"
reason = "Go stores generated compiler and test results in this user cache."
risk_note = "Stop Go builds and tests first; the next invocation must rebuild missing cache entries."
runtime_guard = { process_names = ["go", "compile", "link"] }
[[rules]]
id = "windows-go-build-cache"
label = "Go build cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/go-build", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "trash"
reason = "Go stores generated compiler and test results in this user cache."
risk_note = "Stop Go builds and tests first; the next invocation must rebuild missing cache entries."
runtime_guard = { process_names = ["go", "compile", "link"] }
[[rules]]
id = "windows-pip-download-cache"
label = "pip download cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/pip/Cache", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "trash"
reason = "pip stores downloaded HTTP responses and locally built wheels in this cache."
risk_note = "Stop pip and Python package installation first; future installs may require network access and wheel compilation."
runtime_guard = { process_names = ["pip", "python"] }
[[rules]]
id = "linux-sccache"
label = "sccache compiler cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/sccache", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "trash"
reason = "sccache retains generated compiler outputs in its local disk cache."
risk_note = "Stop the sccache server and builds before review; later compilation may be slower."
runtime_guard = { process_names = ["sccache"] }
[[rules]]
id = "macos-sccache"
label = "sccache compiler cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/Mozilla.sccache", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "trash"
reason = "sccache retains generated compiler outputs in its local disk cache."
risk_note = "Stop the sccache server and builds before review; later compilation may be slower."
runtime_guard = { process_names = ["sccache"] }
[[rules]]
id = "windows-sccache"
label = "sccache compiler cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/Mozilla/sccache", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "trash"
reason = "sccache retains generated compiler outputs in its local disk cache."
risk_note = "Stop the sccache server and builds before review; later compilation may be slower."
runtime_guard = { process_names = ["sccache"] }
[[rules]]
id = "uv-managed-cache"
label = "uv cache (tool managed)"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/uv" }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "inspect"
reason = "uv requires its own cache-management operations and locking; direct filesystem removal is unsafe."
risk_note = "Read-only: use uv cache management after reviewing its separate deletion and recovery behavior. Cleanr does not run it."
[[rules]]
id = "macos-uv-managed-cache"
label = "uv cache (tool managed)"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/uv" }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "inspect"
reason = "uv requires its own cache-management operations and locking; direct filesystem removal is unsafe."
risk_note = "Read-only: use uv cache management after reviewing its separate deletion and recovery behavior. Cleanr does not run it."
[[rules]]
id = "windows-uv-managed-cache"
label = "uv cache (tool managed)"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/uv" }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "inspect"
reason = "uv requires its own cache-management operations and locking; direct filesystem removal is unsafe."
risk_note = "Read-only: use uv cache management after reviewing its separate deletion and recovery behavior. Cleanr does not run it."
[[rules]]
id = "linux-torchinductor-cache"
label = "TorchInductor compiled kernels"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/tmp/torchinductor_?*", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "trash"
reason = "TorchInductor stores generated kernels and graph compilation results in a per-user temporary directory."
risk_note = "Only current-user-owned data is eligible. Stop Python, training and inference tasks; cleaning can trigger expensive recompilation and GPU autotuning."
runtime_guard = { process_names = ["python", "torchrun", "pt_main_thread", "g++", "clang++", "nvcc"], current_user_only = true }
[[rules]]
id = "linux-torchinductor-triton-cache"
label = "TorchInductor Triton kernels"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/tmp/torchinductor_?*/triton", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "trash"
reason = "Triton kernels are nested inside the default TorchInductor cache."
risk_note = "Stop Python, training and inference tasks; kernels must be compiled and tuned again. Overlapping parent and child selections are counted once."
runtime_guard = { process_names = ["python", "torchrun", "pt_main_thread", "g++", "clang++", "nvcc"], current_user_only = true }
[[rules]]
id = "macos-jetbrains-container"
label = "JetBrains retained data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/JetBrains" }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "inspect"
reason = "JetBrains system directories mix generated caches with Local History and other retained data."
risk_note = "Read-only container: only explicitly identified index and log leaves may be reviewed."
inspection_scope = "entry"
[[rules]]
id = "macos-jetbrains-unreviewed-products"
label = "Unreviewed JetBrains product data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/JetBrains/*", exclude_path_globs = ["**/Library/Caches/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*"] }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "inspect"
reason = "This product layout has not been approved for cache-only cleanup."
risk_note = "Read-only: preserve this product directory and all descendants."
[[rules]]
id = "macos-jetbrains-product-container"
label = "JetBrains version data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*" }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "inspect"
reason = "This version directory includes Local History and other state that must be retained."
risk_note = "Read-only container: inspect individual index and log leaves instead."
inspection_scope = "entry"
[[rules]]
id = "macos-jetbrains-retained-data"
label = "JetBrains history and retained state"
category = "developer-cache"
match = { path_glob = "**/Library/Caches/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/*", exclude_path_globs = ["**/Library/Caches/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/index"] }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "inspect"
reason = "Local History and all unreviewed IDE state are retained, including unknown future directories."
risk_note = "Read-only: this data and any parent directory containing it cannot be selected for cleanup."
[[rules]]
id = "macos-jetbrains-index"
label = "JetBrains generated index"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Caches/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/index", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "trash"
reason = "The IDE stores generated file indexes in the documented system index directory."
risk_note = "Quit the IDE first; indexing will run again and can take significant time. Local History is kept."
runtime_guard = { process_names = ["idea", "idea64", "IntelliJ IDEA", "pycharm", "pycharm64", "webstorm", "webstorm64", "java", "fsnotifier"] }
[[rules]]
id = "macos-jetbrains-logs"
label = "JetBrains diagnostic logs"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/Library/Logs/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos"]
action = "trash"
reason = "These are IDE diagnostic logs in the product-specific log location."
risk_note = "Quit the IDE and review any diagnostics you need before cleaning; historical troubleshooting logs cannot be regenerated."
runtime_guard = { process_names = ["idea", "idea64", "IntelliJ IDEA", "pycharm", "pycharm64", "webstorm", "webstorm64", "java", "fsnotifier"] }
[[rules]]
id = "linux-jetbrains-container"
label = "JetBrains retained data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/JetBrains" }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "inspect"
reason = "JetBrains system directories mix generated caches with Local History and other retained data."
risk_note = "Read-only container: only explicitly identified index and log leaves may be reviewed."
inspection_scope = "entry"
[[rules]]
id = "linux-jetbrains-unreviewed-products"
label = "Unreviewed JetBrains product data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/JetBrains/*", exclude_path_globs = ["**/.cache/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*"] }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "inspect"
reason = "This product layout has not been approved for cache-only cleanup."
risk_note = "Read-only: preserve this product directory and all descendants."
[[rules]]
id = "linux-jetbrains-product-container"
label = "JetBrains version data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*" }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "inspect"
reason = "This version directory includes Local History and other state that must be retained."
risk_note = "Read-only container: inspect individual index and log leaves instead."
inspection_scope = "entry"
[[rules]]
id = "linux-jetbrains-retained-data"
label = "JetBrains history and retained state"
category = "developer-cache"
match = { path_glob = "**/.cache/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/*", exclude_path_globs = ["**/.cache/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/index", "**/.cache/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/log"] }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "inspect"
reason = "Local History and all unreviewed IDE state are retained, including unknown future directories."
risk_note = "Read-only: this data and any parent directory containing it cannot be selected for cleanup."
[[rules]]
id = "linux-jetbrains-index"
label = "JetBrains generated index"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/index", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "trash"
reason = "The IDE stores generated file indexes in the documented system index directory."
risk_note = "Quit the IDE first; indexing will run again and can take significant time. Local History is kept."
runtime_guard = { process_names = ["idea", "idea64", "IntelliJ IDEA", "pycharm", "pycharm64", "webstorm", "webstorm64", "java", "fsnotifier"] }
[[rules]]
id = "linux-jetbrains-logs"
label = "JetBrains diagnostic logs"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/log", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["linux"]
action = "trash"
reason = "These are IDE diagnostic logs in the product-specific log location."
risk_note = "Quit the IDE and review any diagnostics you need before cleaning; historical troubleshooting logs cannot be regenerated."
runtime_guard = { process_names = ["idea", "idea64", "IntelliJ IDEA", "pycharm", "pycharm64", "webstorm", "webstorm64", "java", "fsnotifier"] }
[[rules]]
id = "windows-jetbrains-container"
label = "JetBrains retained data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/JetBrains" }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "inspect"
reason = "JetBrains system directories mix generated caches with Local History and other retained data."
risk_note = "Read-only container: only explicitly identified index and log leaves may be reviewed."
inspection_scope = "entry"
[[rules]]
id = "windows-jetbrains-unreviewed-products"
label = "Unreviewed JetBrains product data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/JetBrains/*", exclude_path_globs = ["**/AppData/Local/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*"] }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "inspect"
reason = "This product layout has not been approved for cache-only cleanup."
risk_note = "Read-only: preserve this product directory and all descendants."
[[rules]]
id = "windows-jetbrains-product-container"
label = "JetBrains version data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*" }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "inspect"
reason = "This version directory includes Local History and other state that must be retained."
risk_note = "Read-only container: inspect individual index and log leaves instead."
inspection_scope = "entry"
[[rules]]
id = "windows-jetbrains-retained-data"
label = "JetBrains history and retained state"
category = "developer-cache"
match = { path_glob = "**/AppData/Local/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/*", exclude_path_globs = ["**/AppData/Local/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/index", "**/AppData/Local/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/log"] }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "inspect"
reason = "Local History and all unreviewed IDE state are retained, including unknown future directories."
risk_note = "Read-only: this data and any parent directory containing it cannot be selected for cleanup."
[[rules]]
id = "windows-jetbrains-index"
label = "JetBrains generated index"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/index", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "trash"
reason = "The IDE stores generated file indexes in the documented system index directory."
risk_note = "Quit the IDE first; indexing will run again and can take significant time. Local History is kept."
runtime_guard = { process_names = ["idea", "idea64", "IntelliJ IDEA", "pycharm", "pycharm64", "webstorm", "webstorm64", "java", "fsnotifier"] }
[[rules]]
id = "windows-jetbrains-logs"
label = "JetBrains diagnostic logs"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/AppData/Local/JetBrains/{IntelliJIdea,IdeaIC,PyCharm,PyCharmCE,WebStorm}[0-9]*/log", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["windows"]
action = "trash"
reason = "These are IDE diagnostic logs in the product-specific log location."
risk_note = "Quit the IDE and review any diagnostics you need before cleaning; historical troubleshooting logs cannot be regenerated."
runtime_guard = { process_names = ["idea", "idea64", "IntelliJ IDEA", "pycharm", "pycharm64", "webstorm", "webstorm64", "java", "fsnotifier"] }
[[rules]]
id = "conda-package-archives"
label = "Conda package download archive"
category = "developer-cache"
match = { kind = "file", path_glob = "**/{.conda,miniconda3,anaconda3,miniforge3,Miniconda3,Anaconda3,Miniforge3}/pkgs/*.{conda,tar.bz2}", min_size = 1048576, parent_marker = "urls.txt" }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "trash"
reason = "This compressed package download is inside a standard Conda package cache with a urls.txt marker."
risk_note = "Stop Conda and Mamba transactions; preserve offline packages you still need. Extracted packages and environments are retained."
runtime_guard = { process_names = ["conda", "mamba", "micromamba", "python"] }
[[rules]]
id = "conda-package-cache-container"
label = "Conda retained package cache"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/{.conda,miniconda3,anaconda3,miniforge3,Miniconda3,Anaconda3,Miniforge3}/pkgs" }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "inspect"
reason = "Conda package caches include extracted packages that may be linked by installed environments."
risk_note = "Read-only container: only compressed downloads with cache evidence may be reviewed."
inspection_scope = "entry"
[[rules]]
id = "conda-retained-packages"
label = "Conda extracted packages and metadata"
category = "developer-cache"
match = { path_glob = "**/{.conda,miniconda3,anaconda3,miniforge3,Miniconda3,Anaconda3,Miniforge3}/pkgs/*", exclude_path_globs = ["**/{.conda,miniconda3,anaconda3,miniforge3,Miniconda3,Anaconda3,Miniforge3}/pkgs/*.{conda,tar.bz2}"] }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "inspect"
reason = "Extracted packages, metadata and unknown cache contents may be needed by Conda environments."
risk_note = "Read-only: environments and extracted package directories are not download archives."
[[rules]]
id = "huggingface-container"
label = "Hugging Face retained data"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/huggingface" }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "inspect"
reason = "Hugging Face caches can contain model weights, datasets, references and credentials."
risk_note = "Read-only container: only the separate Xet download chunk cache is eligible for review."
inspection_scope = "entry"
[[rules]]
id = "huggingface-retained-data"
label = "Hugging Face models and retained data"
category = "developer-cache"
match = { path_glob = "**/.cache/huggingface/*", exclude_path_globs = ["**/.cache/huggingface/xet"] }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "inspect"
reason = "Models, datasets, tokens and unknown cache contents are retained."
risk_note = "Read-only: model blobs, shared references and download remnants are outside these rules."
[[rules]]
id = "huggingface-xet-container"
label = "Hugging Face Xet retained container"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/huggingface/xet" }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "inspect"
reason = "Xet containers mix download chunks, upload shards and resumable staging."
risk_note = "Read-only container: only a chunk_cache directory may be reviewed."
inspection_scope = "entry"
[[rules]]
id = "huggingface-xet-environment"
label = "Hugging Face Xet retained container"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/huggingface/xet/*", exclude_path_globs = ["**/.cache/huggingface/xet/chunk_cache"] }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "inspect"
reason = "Xet containers mix download chunks, upload shards and resumable staging."
risk_note = "Read-only container: only a chunk_cache directory may be reviewed."
inspection_scope = "entry"
[[rules]]
id = "huggingface-xet-retained-data"
label = "Hugging Face Xet upload state"
category = "developer-cache"
match = { path_glob = "**/.cache/huggingface/xet/*/*", exclude_path_globs = ["**/.cache/huggingface/xet/*/chunk_cache", "**/.cache/huggingface/xet/chunk_cache/*"] }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "inspect"
reason = "Xet upload shards, resumable staging and unknown environment contents are retained."
risk_note = "Read-only: uploads and staging are not disposable download chunks."
[[rules]]
id = "huggingface-xet-legacy-retained-data"
label = "Hugging Face Xet legacy upload state"
category = "developer-cache"
match = { path_glob = "**/.cache/huggingface/xet/{shard_cache,staging,logs}" }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "inspect"
reason = "Xet upload state and diagnostics are retained."
risk_note = "Read-only: this location is not the download chunk cache."
[[rules]]
id = "huggingface-xet-legacy-chunks"
label = "Hugging Face Xet download chunks"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/huggingface/xet/chunk_cache", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "trash"
reason = "This optional Xet cache stores reusable downloaded chunks separately from models and upload state."
risk_note = "Stop Hugging Face downloads and Python jobs first; removed chunks may need downloading again. Upload staging and model files are kept."
runtime_guard = { process_names = ["python", "hf", "huggingface-cli", "git-xet", "xet"] }
[[rules]]
id = "huggingface-xet-chunks"
label = "Hugging Face Xet download chunks"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/.cache/huggingface/xet/*/chunk_cache", min_size = 1048576 }
confidence = "medium"
default_selected = false
platforms = ["macos", "linux", "windows"]
action = "trash"
reason = "This optional Xet cache stores reusable downloaded chunks separately from models and upload state."
risk_note = "Stop Hugging Face downloads and Python jobs first; removed chunks may need downloading again. Upload staging and model files are kept."
runtime_guard = { process_names = ["python", "hf", "huggingface-cli", "git-xet", "xet"] }

[[rules]]
id = "conda-installation-container"
label = "Conda installation and environments"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/{.conda,miniconda3,anaconda3,miniforge3,Miniconda3,Anaconda3,Miniforge3}" }
confidence = "medium"
default_selected = false
action = "inspect"
inspection_scope = "entry"
reason = "Conda installations and user state include environments and retained package data."
risk_note = "Read-only container: only verified compressed package downloads may be reviewed."

[[rules]]
id = "conda-retained-environments"
label = "Conda environments and installation data"
category = "developer-cache"
match = { path_glob = "**/{.conda,miniconda3,anaconda3,miniforge3,Miniconda3,Anaconda3,Miniforge3}/*", exclude_path_globs = ["**/{.conda,miniconda3,anaconda3,miniforge3,Miniconda3,Anaconda3,Miniforge3}/pkgs"] }
confidence = "medium"
default_selected = false
action = "inspect"
reason = "Conda environments, configuration and installed tools are retained."
risk_note = "Read-only: installed environments and their dependencies cannot be selected for cleanup."

[[rules]]
id = "conda-extracted-package-directories"
label = "Conda extracted package directories"
category = "developer-cache"
match = { kind = "directory", path_glob = "**/{.conda,miniconda3,anaconda3,miniforge3,Miniconda3,Anaconda3,Miniforge3}/pkgs/*" }
confidence = "medium"
default_selected = false
action = "inspect"
reason = "Extracted package directories can be linked by installed environments, regardless of their filename suffix."
risk_note = "Read-only: only regular compressed archive files with a package-cache marker may be reviewed."
