[package]
name        = "cleanr-tasks"
description = "Cleanup execution and restore manifest management for cleanr"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
anyhow.workspace = true
chrono.workspace = true
cleanr-config = { version = "0.15.0", path = "../config" }
cleanr-core = { version = "0.15.0", path = "../core" }
cleanr-fs = { version = "0.15.0", path = "../fs" }
cleanr-rules = { version = "0.15.0", path = "../rules" }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
sysinfo.workspace = true
tempfile.workspace = true
trash.workspace = true
uuid.workspace = true

[target.'cfg(target_os = "macos")'.dependencies]
objc2-foundation.workspace = true
rustix = { version = "1.1.4", features = ["fs"] }

[lints]
workspace = true
